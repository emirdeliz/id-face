# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

PORT=3000
ID_FACE_HOST=
GET_SESSION_URL=/login.fcgi
CREATE_USER_URL=/create_objects.fcgi?session=
CREATE_QR_CODE_URL=/create_objects.fcgi?session=
